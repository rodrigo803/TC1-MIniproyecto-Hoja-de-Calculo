// Archivo: src/sistema.lucia
import "interfaz";
import "evaluador";

func ejecutarSistema(): void {
    let matrizFormulas = [];
    let matrizNormal = [];
    
    let i = 0;
    while (i < 100) {
        matrizFormulas.append(" ");
        matrizNormal.append("0");
        i = i + 1;
    }
    print("Matriz 10x10 lista en memoria.");
    
    let ejecutable = true;
    let opcion = ""; 

    while (ejecutable) {
        // Directo, sin prefijos
        mostrarMenu();
        opcion = input("Elige una opción: "); 
        
        if (opcion == "1") {
            print("\n--- ASIGNAR VALOR ---");
            let fila_str = input("Ingresa la fila (1 a 10): ");
            let col_str = input("Ingresa la columna (1 a 10, 1=A, 2=B...): ");
            let valor = input("Ingresa el valor o fórmula: ");
            
            let fila = int(fila_str);
            let col = int(col_str);
            let indice = (fila - 1) * 10 + (col - 1);
            
            matrizFormulas[indice] = valor;
            
            // Directo a la función del evaluador
            matrizNormal[indice] = evaluarCelda(valor, matrizNormal); 
            
            print("¡Valor asignado correctamente!");
            
        } else if (opcion == "2") {
            // Directo a la función de la interfaz
            mostrarMatriz(matrizFormulas, "MATRIZ DE FÓRMULAS");
        } else if (opcion == "3") {
            mostrarMatriz(matrizNormal, "MATRIZ NORMAL");            
        } else if (opcion == "4") {
            print("Cerrando la hoja de cálculo...");
            ejecutable = false; 
        } else {
            print("Opción no válida.");
        }
    }
}