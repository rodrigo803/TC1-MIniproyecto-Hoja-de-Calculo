switch (day) {
    case 1: print("Mon");
    case 2: print("Tue");
    default: print("Other");
}

for (let i: int = 0; i < 3; i++) { print(i); }
while (ready) { break; }
do { continue; } while (false);